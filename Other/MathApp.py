import random
from sys import exit

class Engine(object):
    def __init__(self, kind):
        self.kind = kind

    def go(self):
        if kind == ""


class Operation(object):


class Begin(Operation):
    ls = ["exponential fucntion", "exponential growth", "exponential decay", "compund inetrest"]

    def start(self):
        print("Hello there, this will solve math problems. What would you like to solve for? %d" % ls)

    return Engine()
